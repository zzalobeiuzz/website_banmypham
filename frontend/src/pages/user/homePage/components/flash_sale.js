import React from 'react';
import Select from './selection';

const FlashSale = () => {
    const Title = "Flash Sale";

    return (
        <Select title={Title} />
    );
};

export default FlashSale;