import React from 'react';
import FlashSale from './flash_sale';

const top_brand = () => {
    const brandTitle = "Thương hiệu nổi bật";

    return (
        <FlashSale title={brandTitle} />
    );
};

export default top_brand;