import React from 'react';
import Select from './selection';

const FlashSale = () => {
    const brandTitle = "Flash Deal";

    return (
        <Select title={brandTitle} />
    );
};

export default FlashSale;