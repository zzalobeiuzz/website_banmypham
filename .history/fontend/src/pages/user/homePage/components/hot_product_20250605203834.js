import React from 'react'
import { connect } from 'react-redux'

export const hot_product = (props) => {
  return (
    <div>hot_product</div>
  )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(hot_product)