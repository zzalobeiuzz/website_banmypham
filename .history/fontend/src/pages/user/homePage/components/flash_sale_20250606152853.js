import React from 'react';
import Select from './select';

const top_brand = () => {
    const brandTitle = "Thương hiệu nổi bật";

    return (
        <SelectlashSale title={brandTitle} />
    );
};

export default top_brand;