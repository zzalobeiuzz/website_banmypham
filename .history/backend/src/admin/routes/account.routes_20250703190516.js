//============= Khai báo biến và các hàm
const express = require("express");
const router = express.Router();
const{verifyToken,verifyAdmin} = requestIdleCallback
// Định nghĩa các route trong Express Router
router.get("/",);
module.exports = router;